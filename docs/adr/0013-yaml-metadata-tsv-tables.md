# YAML metadata and TSV tables

The registry uses YAML for small identity, metadata, configuration, validation, and errata files, and TSV for large tabular Source Inventories and Release Manifest analysis tables. This keeps configuration readable while keeping large analysis lists diffable, streamable, and easy to consume from R and Python.
