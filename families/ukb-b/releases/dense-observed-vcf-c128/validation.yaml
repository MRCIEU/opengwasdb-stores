status: failed
validated_at: "2026-08-06T05:39:00Z"
validator:
  name: opengwasdb validate
checks:
  schema: not_run
  files: not_run
  reader_smoke_test: not_run
reports:
  opengwasdb_validate: >
    error: top-hit index p_5e_04 is missing analysis_offsets
    error: top-hit index p_5e_06 is missing analysis_offsets
    error: top-hit index p_5e_08 is missing analysis_offsets
notes: >
  Run directly against the real store artifact
  (/data/opengwasdb/wip/ukb-b-c128.opengwasdb), not against this reverse-
  engineered manifest — this registry has no local copy of the source VCFs
  to run a manifest-level schema/files check against. The three errors are
  pre-existing: this session never wrote to ukb-b-c128.opengwasdb, only read
  from it (as the source store for the ukb-b-c128-completed rebuild, issue
  #34). Worth investigating separately — the store's core z/se/variant data
  reads fine (the completed-store rebuild consumed it successfully and
  passed its own validation), so this looks like an incomplete or stale
  top-hit index build rather than corrupt underlying data.
