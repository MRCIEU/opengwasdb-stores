metadata_schema_version: 1
store_family_id: ukb-b
family_release_id: dense-observed-vcf-c128
status: candidate
source_collection_id: opengwas-gwas-vcf
release_kind: source-natural
association_coverage: full_gwas
description: >
  Candidate release bundle reverse-engineered for the ukb-b-c128 Dense
  Observed-Only store already built and in use at
  /data/opengwasdb/wip/ukb-b-c128.opengwasdb. Reconstructed from
  explodecomputer/opengwasdb data/ukb-b/ (README.md, build.sh, manifest.tsv
  @ blob be8b8d9ee38c5cae3a069007cd110363d1715bc6) rather than from a
  Manifest Generator run against source files, because the 2,514 source
  GWAS-VCFs live on a separate server this registry does not have direct
  access to.
created_at: "2026-08-06T00:00:00Z"
accepted_at: null
generator:
  name: manual reverse-engineering from explodecomputer/opengwasdb data/ukb-b
  version: manifest.tsv blob be8b8d9ee38c5cae3a069007cd110363d1715bc6
source_defaults:
  source_genome_build: GRCh37
  license: null
lineage:
  derived_from: null
notes: >
  Reverse-engineered, not generator-produced — see analyses.tsv for exactly
  which columns are populated per row. checksum/checksum_algorithm/size_bytes
  are blank for every row: filling them needs reading the source VCFs, which
  a sha256sum/stat pass run on the remote host (no data copied locally)
  could do without the file transfer this reverse-engineering was meant to
  avoid. license is null, matching source-collections/opengwas-gwas-vcf's
  own unset default_license. original_effect_scale/stored_effect_scale are
  blank per row because the manifest does not distinguish continuous vs.
  binary UKB-b traits, so scale (sd vs log_or) cannot be assigned without
  inspecting each trait. ancestry_assignment_method is explicitly
  "unassigned" (AF-based ancestry assignment, issue #23, has not been run
  against this family). sample_size_kind="total" and
  sample_size_scope="analysis_level" are inferred from the manifest's single
  per-trait `n` column, not confirmed against source file headers.

  The completed store built from this observed store
  (ukb-b-c128-completed.opengwasdb, opengwasdb-stores issue #34) has its own
  provenance note at /data/opengwasdb/wip/ukb-b-c128-completed.PROVENANCE.md.
