status: failed
validated_at: '2026-08-19T00:00:00Z'
validator:
  name: opengwasdb validate / resources/lib/schema_validate.py
  version: opengwasdb@7e755d1da6565bebe2b5807582dc70b9c10e1970
checks:
  schema: failed
  files: passed
  reader_smoke_test: passed
reports: []
warnings: []
errors:
- "schema_validate.py: 2,514/2,514 analyses have no value for required column 'sample_size_kind'"
- "schema_validate.py: 2,514/2,514 analyses have no value for required column 'sample_size_scope'"
- "schema_validate.py: 2,514/2,514 analyses have no value for required column 'sample_size'"
- "schema_validate.py: 2,514/2,514 analyses have no value for required column 'original_effect_scale'"
- "schema_validate.py: 2,514/2,514 analyses have no value for required column 'ancestry_assignment_method'"
- "schema_validate.py: 1,846/2,514 analyses have stored_effect_scale='log_or' but no n_cases"
- "schema_validate.py: 1,846/2,514 analyses have stored_effect_scale='log_or' but no n_controls"
notes: |
  checks.reader_smoke_test here stands in for `opengwasdb validate` against the real store artifact (`/data/opengwasdb/stores/ukb-b-c128-completed.opengwasdb`), which reports `valid` -- the store's zarr arrays, index, and analyses.tsv row count are all internally consistent and the store reads back correctly. checks.files is `passed`: the registered artifacts.store_uri exists and matches this bundle's provenance.

  checks.schema is a genuine, pre-existing failure, not new: `sample_size_kind`/`sample_size_scope`/`sample_size`/`original_effect_scale`/`ancestry_assignment_method` were never populated for this batch (same gap as the parent `dense-observed-vcf-c128` release, which `dense-observed-vcf-c128-resolved` fixes only at the manifest level -- the real VCF-manifest builder never consumes those resolved values, opengwasdb#14, unresolved). Overall `status: failed` reflects this honestly rather than hiding a hard required-column gap behind `passed_with_warnings`. It is not a regression from this release's Reference Completion or analyses.tsv schema migration (opengwasdb#103, fixed) -- both of those are independently confirmed working by the `reader_smoke_test` result above.
